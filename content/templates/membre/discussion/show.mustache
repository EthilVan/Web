{{#title}}Discussion{{/title}}

<div class="titre">{{ name }}</div>
{{# modo? }}
   <a href="{{ edit_url }}">Editer</a>
   <a class="need-confirmation" href="{{ delete_url }}">Supprimer</a>
{{/ modo? }}
<div class="ligne"></div>
<div class="date">Discussion crée le {{ date }} par {{ author }}</div>

{{# pager }}
{{> membre/discussion/_pager }}
{{/ pager }}

<div class="cleaner"></div>

<div class="messages">
{{# messages }}
   {{> membre/discussion/_message }}
{{/ messages }}
</div>

{{# pager }}
{{> membre/discussion/_pager }}
{{/ pager }}

<div id="champ-reponse">
   <a class="btn pull-right" href="{{ response_url }}"
         data-inline-target="#champ-reponse">
      Répondre
   </a>
</div>
